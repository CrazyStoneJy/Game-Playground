import gen from '../src/mine-sweep/gen';
import genGrid from '../src/mine-sweep/gen';

describe('test', () => {
    test('gen', () => {
        const res = gen(10 ,10);
        console.log(res);
        
    });
});